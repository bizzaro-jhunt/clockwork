List of Things To Do
====================

userdb::
  File locking (shadow)

policy::
  Memory Management Changes
  policy support in proto

parser::
  if-else-if-else construct

COLLAPSE MODULES:
  RESOURCES:  res_user + res_file + res_group
  LIST:       hash + stringlist + list
