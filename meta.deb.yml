name: clockwork
version: 0.2.8
Architecture: amd64
Depends:
  - ${shlibs:Depends}
Build-Depends:
  - libssl-dev
  - libaugeas-dev
  - libsqlite3-dev
  - libreadline-dev
  - pkg-config
  # For functional tests
  - expect
  - flex
  - bison
